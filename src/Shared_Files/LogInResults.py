# Shared_Files/player_account.py (or another file where appropriate)

class LoginResult:
    def __init__(self, session_token, player_account):
        self.session_token = session_token
        self.player_account = player_account
